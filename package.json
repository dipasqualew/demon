{
  "name": "demon",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/review-app && npm run build -w packages/playwright && npm run build -w packages/mcp-server"
  }
}
